<form method="post" action="{{ path('app_guarantor_delete', {'id': guarantor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guarantor.id) }}">
    <button class="btn">Delete</button>
</form>
