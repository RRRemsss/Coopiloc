{% extends 'base.html.twig' %}

{% block title %}PersonDetail{% endblock %}

{% block body %}
    <h1>PersonDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ person_detail.id }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ person_detail.lastname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ person_detail.firstname }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ person_detail.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ person_detail.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_person_detail_index') }}">back to list</a>

    <a href="{{ path('app_person_detail_edit', {'id': person_detail.id}) }}">edit</a>

    {{ include('person_detail/_delete_form.html.twig') }}
{% endblock %}
