<form method="post" action="{{ path('app_identity_document_delete', {'id': identityLeaseParty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ identityLeaseParty.id) }}">
    <button class="btn">Delete</button>
</form>
