<form method="post" action="{{ path('app_lease_party_delete', {'id': lease_party.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lease_party.id) }}">
    <button class="btn">Delete</button>
</form>
